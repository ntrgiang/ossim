package simpleoverlay.applications.peer;

import inet.applications.udpapp.UDPBasicApp;

simple PeerBootstrap extends UDPBasicApp
{
    parameters:
        @display("i=misc/globe,red");
        @class(PeerBootstrap);
        messageLength = default(4B);
        messageFreq = default(1s);
        
//        localPort = default(9000);
//        destPort = default(8083);
//        destAddresses = default("bootstrap");
        
        // parent's parameters
        // all threads will be activelly created
//        serverThreadClass = "n/a";

        // own parameters
        bool debugFlag = default(false);
//        bool subModulesDebugFlag = default(false);
//        string connectAddress = default("bootstrap");  // bootstrap address
//        int connectPort = default(8083); //The port number to connect to
        // The number of Peers the Client requests to the Tracker
//        int numWant = default(50);
        double refreshInterval @unit(s) = default(180s);
}

